import styled from "styled-components";

export const Wrapper = styled.div`
  /* CSS样式（支持嵌套样式） */
  .backtop {
    position: fixed;
    width: 49px;
    height: 44px;
    bottom: 160px;
    margin-left: 50px;
    background-position: -265px -47px;
  }
`;
