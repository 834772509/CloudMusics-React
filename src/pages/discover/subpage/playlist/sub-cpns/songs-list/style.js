import styled from "styled-components";

export const Wrapper = styled.div`
  /* CSS样式（支持嵌套样式） */
  margin-top: 30px;

  .play-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
  }

`;
